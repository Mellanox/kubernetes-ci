{%- for pf_info in pf_infos %}

SUBSYSTEM=="net", ACTION=="add|move", ATTRS{phys_switch_id}=="{{ pf_info.phys_switch_id }}", ATTR{phys_port_name}=="pf{{ pf_info.phys_port_name[1:] }}vf*", IMPORT{program}="/etc/udev/switchdev-vf-link-name.sh $attr{phys_port_name}", NAME="{{ pf_info.name }}_$env{NUMBER}"

{%- endfor %}
